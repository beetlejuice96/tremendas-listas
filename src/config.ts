/** Interruptores de funcionalidad de la app. */

/**
 * Curaduría (votación y cruce de votos).
 *
 * Apagada a pedido del equipo: la sección escribe votos en la base y se quiso
 * evitar que alguien entre por error antes de que empiece la curaduría de la
 * próxima edición. Para volver a habilitarla, poner `true`: el código sigue
 * completo, sólo queda inalcanzable desde la interfaz.
 */
export const CURADURIA_HABILITADA = false
